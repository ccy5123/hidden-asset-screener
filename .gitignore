# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg
build/
dist/
.eggs/

# Virtual envs
.venv/
venv/
env/

# Test / coverage
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml

# Local cache & artifacts (SPEC-CORE: local cache, reports)
# NOTE: anchored to repo root so they never match the source package src/asset_play/cache/.
/.cache/
/cache/
*.sqlite
*.sqlite3
*.parquet
/out/
/reports/

# Secrets — DART / data.go.kr / V-World keys
.env
.env.*
!.env.example

# Streamlit deployment secrets (배포 키) — 예시(.example)만 커밋
.streamlit/secrets.toml

# Editors / OS
.idea/
.vscode/
.DS_Store
